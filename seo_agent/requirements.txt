# Core
requests>=2.31
beautifulsoup4>=4.12
scikit-learn>=1.3
openpyxl>=3.1
python-dotenv>=1.0

# Pick the LLM provider you use (install at least one):
replicate>=0.25        # LLM_PROVIDER=replicate  (you have this)
anthropic>=0.39        # LLM_PROVIDER=anthropic  (best copy quality)
openai>=1.40           # LLM_PROVIDER=openai
